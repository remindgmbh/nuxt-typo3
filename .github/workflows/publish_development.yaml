name: Publish Development

on:
  push:
    branches:
      - "develop"

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: remindgmbh/npm-publish-dev-action@v1.0.0
        with:
          npm-token: ${{ secrets.NPM_TOKEN }}
