name: Publish Development

on:
  push:
    branches:
      - 'develop'

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: remindgmbh/npm-publish-dev-action@main
        with:
          npm-token: ${{ secrets.NPM_TOKEN }}
